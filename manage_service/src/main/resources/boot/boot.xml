<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.2.xsd  
           http://www.springframework.org/schema/aop   
           http://www.springframework.org/schema/aop/spring-aop-3.2.xsd  
           http://www.springframework.org/schema/tx  
           http://www.springframework.org/schema/tx/spring-tx-3.2.xsd  
           http://www.springframework.org/schema/context  
           http://www.springframework.org/schema/context/spring-context-3.2.xsd"
       default-autowire="byName" default-lazy-init="false">
    <!-- 读入配置属性文件 -->
    <context:property-placeholder
            ignore-unresolvable="true"
            file-encoding="UTF-8"
            location="classpath*:/config/*.properties"/>

    <import resource="classpath*:/boot/dubbo-provider.xml"/>
    <!--<import resource="classpath*:/config/applicationContext_rabbitmq.xml"/>-->
    <!--<import resource="classpath*:/config/applicationContext_rabbitmq_client.xml"/>-->

    <bean id="confirmLoanTask" class="com.zw.miaofuspd.timedTask.service.ConfirmLoanTask" p:taskName="定时跑批（确认放款计算）" p:cron="0 0/30 * * * ?" />
    <!--<bean id="confirmLoanTask" class="com.zw.miaofuspd.timedTask.service.ConfirmLoanTask" p:taskName="定时跑批（确认放款计算）" p:cron="*/30 * * * * ?" />-->
    <bean id="AutomaticRepaymentTask" class="com.zw.miaofuspd.timedTask.service.InterestReductionTask" p:taskName="定时跑批（利息、费用、逾期计算）" p:cron="0 0 2 * * ?" />
    <!--<bean id="interestReductionTask" class="com.zw.miaofuspd.timedTask.service.InterestReductionTask" p:taskName="定时跑批（利息、费用、逾期计算）" p:cron="*/5 * * * * ?" />-->


    <!-- 同盾配置BEAN create by 陈清玉-->
    <bean id="tongDunSettings" class="com.api.model.tongdun.TongDunSettings">
        <property name="reportIdUrl" value="${tongdun.reportIdUrl}"/>
        <property name="reportInfoUrl" value="${tongdun.reportInfoUrl}"/>
        <property name="partnerCode" value="${tongdun.partnerCode}"/>
        <property name="partnerKey" value="${tongdun.partnerKey}"/>
        <property name="appName" value="${tongdun.appName}"/>
    </bean>
</beans>